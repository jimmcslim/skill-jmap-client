# JMAP Configuration
# Copy this file to .env and fill in your credentials

# JMAP server hostname (without https://)
# For Fastmail: api.fastmail.com
# For other providers, check their JMAP documentation
JMAP_HOST=api.fastmail.com

# API token (read-only)
# For Fastmail: Create an app password at Settings → Password & Security → App Passwords
# Used for listing folders and reading emails
# For other providers: Use your account password or API token
JMAP_API_TOKEN_RO=your-read-only-token-here

# API token (read-write)
# For Fastmail: Create a separate app password with read-write access
# Used for creating folders and modifying email data (jmap_create_folder.py)
# For other providers: Use an API token with write permissions
JMAP_API_TOKEN_RW=your-read-write-token-here
